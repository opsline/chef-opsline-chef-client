#!/bin/bash

lockfile="<%= node['opsline-chef-client']['disabled_file'] %>"
if [ -f $lockfile ]; then
    rm -f $lockfile
    echo "chef has been enabled"
else
    echo "chef has already been enabled"
fi
